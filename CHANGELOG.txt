Made I.T. Theme Changelog
==========================

Version 3.0.9 - 2026-06-26
-------------------------
Added
- Added root block workspace manifest `gutenberg/blocks/package.json` with shared `build` and `start` scripts for Gutenberg blocks.

Changed
- Changed multiple Gutenberg and theme asset registrations to use `MADEIT_VERSION` instead of per-file `filemtime(...)` cache busting (`functions.php`, `gutenberg/loader.php`, `inc/cookieconsent.php`, block init files).
- Changed editor script loading to editor-only hooks by moving `madeit_block_editor_styles` and `madeit_extend_gutenberg` from `enqueue_block_assets` to `enqueue_block_editor_assets`.
- Updated block package manifests/lockfiles with dependency bumps, including `js-yaml` 4.2.0, `form-data` 4.0.6, `launch-editor` 2.14.1, `shell-quote` 1.8.4, and `axios` 1.17.0.
- Regenerated Gutenberg build asset metadata files to normalized PHP array syntax in multiple `build/*.asset.php` files.
- Applied code-style normalization in several WP-CLI and helper files without intended behavioral changes (`inc/wpcli/*`, `inc/call.php`, `inc/categorie-seo-pages.php`, `inc/core/popup/includes/fields.php`).

Fixed
- Fixed frontend loading of editor-only JavaScript dependencies (`wp-block-editor`, `wp-api-fetch`, `wp-data`) by removing global enqueue and constraining it to block editor context.
- Fixed image and separator extension enqueue behavior so frontend only receives CSS, while editor-only JavaScript stays in editor hooks.

Removed
- Removed broad `enqueue_block_assets` usage for editor-only scripts in key theme bootstrap paths.

Notes
- Comparison scope: `3.0.8..HEAD`
- Files changed: 35 (295 insertions, 233 deletions)
- Working tree status before release prep: clean (no uncommitted changes)

Version 3.0.8 - 2026-06-17
-------------------------
Added
- Added WPML config for custom post type `categorie-pagina` in `wpml-config.xml`
- Added new WP-CLI command `madeit merge-product-cat` to safely merge product categories (supports dry-run/apply, recursive child merge, and product reassignment)

Changed
- Refactored category SEO page rendering in `inc/categorie-seo-pages.php` into reusable helpers for query/render flow
- Added WPML-aware lookup and fallback behavior for category SEO pages (current language, default language term lookup, translated post fallback)
- Updated duplicate category slug WP-CLI command to a safer workflow with explicit apply mode, WPML language/trid-aware classification, and richer reporting
- Extended duplicate slug WP-CLI command with options for language filtering, default language priority, and optional orphan parent repair
- Changed Odoo category path parsing and category reuse logic to better handle spaced separators and existing duplicate terms
- Updated mobile menu fallback breakpoint in top navigation from `md` to `lg`
- Added theme constant `MADEIT_CATEGORIE_SEO_PAGES` defaulting to enabled

Fixed
- Fixed Odoo category matching to always use the lowest `term_id` when duplicate matches exist for the same segment/parent
- Fixed potential stale category resolution in Odoo sync flow by disabling transient reads/writes for category path lookup

Removed
- No removed files or features in this release window

Notes
- Comparison scope: `3.0.7..HEAD`
- Commits included:
  - `656fc654` Improve SEO category pages
  - `555e60d0` Update

Version 3.0.7 - 2026-06-10
-------------------------
Added
- Added legacy boxed inline wrapper save handling for the Container Content block
- Added WP-CLI command to fix duplicate product category slugs

Changed
- Changed Container Content migration eligibility to detect legacy boxed inline wrappers
- Changed legacy saveV5 handling to preserve inline wrapper styles and avoid new CSS vars on legacy markup
- Changed starter content defaults to omit bundled widgets, thumbnails, and theme mods
- Updated under-construction and theme image assets (loader, templates, screenshot)

Fixed
- Fixed product category inserts during Odoo sync by letting WordPress generate unique slugs

Removed
- Removed bundled starter content attachments and sample images

Version 3.0.6 - 2026-06-09
-------------------------
Added
- Added class name and style support for the Container Content Column block in the editor

Changed
- Changed Container Content alignment handling so only full-width containers set `data-align="full"`
- Changed Container Content migration eligibility to require frontend wrapper or row markers
- Changed security auto-update defaults so core, plugin, and theme updates are enabled by default
- Changed auto-update gating to accept nullable core decisions while enforcing the update window

Fixed
- Fixed container width adjustments on blog index items by skipping negative-margin corrections
- Fixed frontend container auto-fix behavior to avoid blog post styling conflicts
- Fixed core image style merging by applying responsive styles safely and stripping layout objects
- Fixed setup wizard initialization by removing an unconditional `opcache_reset()` call

Removed
- Removed generated source maps for Container Content block build assets

Version 3.0.5 - 2026-06-08
-------------------------
Added
- Added custom responsive unit value attributes for Container Content block min-height and max-width across desktop, tablet, and mobile breakpoints
- Added legacy save-version export and matching deprecated migration path (`saveV5`) for older container markup with inline margin styles
- Added security settings and hardening dashboard support for WordPress core auto-updates
- Added generated source maps for Container Content block build assets (`index.css.map`, `index.js.map`, and `style-index.css.map`)

Changed
- Changed Container Content block editor and save behavior to support `__custom__` units via dedicated custom-value fields in `AdvancedUnitSelect`
- Changed Container Content frontend sizing CSS so multi-column max-width now accounts for row-gap spacing
- Changed security auto-update handling to run core/plugin/theme updates only within the configured time window (06:00-17:00 site timezone)
- Changed security defaults for core, plugin, and theme auto-updates to enabled
- Changed daily call telemetry to include disk size, free space, and used space metrics
- Changed support-session notification subject to include site URL context

Fixed
- Fixed duplicate blocked IP handling by switching to SQL upsert logic in IP manager (`INSERT ... ON DUPLICATE KEY UPDATE`)
- Fixed tracking and analytics first-item selection by using `array_first(...) ?? ''` fallbacks instead of direct index access
- Fixed legacy Container Content block migration eligibility rules to better separate inline-margin legacy markup cases
- Fixed remote update action in daily call flow by implementing theme update execution with `Theme_Upgrader`

Removed
- No removed files or features in this release window

Notes
- Comparison scope uses `3.0.4` versus current master working state for `3.0.5`.
- `style.css` and `README.txt` version bump markers are currently present as uncommitted working-tree changes.

Version 3.0.4 - 2026-06-04
-------------------------
Added
- Added WP-CLI commands to list blocked IPs, block IPs, unblock IPs, and whitelist IPs in the security module

Changed
- Updated popup frontend behavior to support specific-page auto-display and improved modal close handling across Bootstrap and fallback flows
- Updated popup ACF field behavior so page selection is conditionally shown for the specific_pages action with clearer admin instructions
- Updated popup markup rendering to avoid forcing immediate modal display in frontend HTML output
- Updated blocked IP page styling with improved layout and request form states
- Updated Color Image block metadata/versioning and regenerated build assets for migration compatibility
- Updated editor outside-root block guard handling in editor restriction script

Fixed
- Fixed Color Image block deprecated margin migration so positive offsets no longer generate invalid wrapper margins
- Fixed WooCommerce login compatibility in honeypot login protection by skipping wp-login timing token checks for WooCommerce form flows
- Added unblock request support on blocked IP page with nonce validation, rate limiting, and admin email notifications

Notes
- Includes new security operations tooling through WP-CLI:
  - `wp madeit-security list-blocked`
  - `wp madeit-security block-ip <ip> [--reason="Manual block"] [--duration=3600]`
  - `wp madeit-security unblock-ip <ip>`
  - `wp madeit-security whitelist-ip <ip> [--label="Office IP"]`

Version 3.0.3 - 2026-06-03
-------------------------
Fixed
- Removed stray output from under construction routing to prevent blank lines before XML responses

Notes
- Patch release to avoid extra whitespace in sitemap and other XML outputs.

Version 3.0.2 - 2026-06-03
-------------------------
Added
- Added generated filters/hooks export metadata in `bin/filters-hooks-2.10.10-2026-06-03-1225.json`

Changed
- Fixed frontend Container block rendering and wrapper handling in `assets/js/script.js`
- Improved Separator block attribute defaults and fallback handling in `inc/core/separator-extension/assets/separator.js`
- Preserved existing `node_modules` in `bin/build-node-projects.sh` when rebuilding node subprojects
- Updated generated Gutenberg block build output for `advanced-controls`, `madeit-card`, `madeit-container`, `madeit-maps`, `madeit-review`, `madeit-slider`, and `madeit-tabs`
- Bumped theme version to `3.0.2` in `style.css`

Notes
- Patch release with frontend block fixes, separator stability improvements, and build tooling refinements.

Version 3.0.1 - 2026-06-03
-------------------------
Added
- New Git changelog and release tooling with `.github/skills/project-git-change-log/SKILL.md`
- New CLI tooling in `bin/` for building node subprojects and exporting WordPress filters/hooks
- New theme screenshot image `screenshot.png`
- Additional Gutenberg block save-version compatibility files for Container blocks

Changed
- Updated Gutenberg blocks and editor code across the theme: `madeit-card`, `madeit-color-image-block`, `madeit-container`, `madeit-maps`, `madeit-review`, `madeit-slider`, `madeit-tabs`, and shared editor components
- Updated generated block build output and package lock files for multiple Gutenberg blocks
- Updated editor support scripts: `assets/js/ai-sidebar.js`, `assets/js/editor-restrict-blocks.js`, and `assets/js/gutenberg.js`
- Updated theme assets including `assets/bootstrap-5/style.css`, `assets/css/gutenberg.css`, and `style.css`
- Updated admin notice and update handling in `gutenberg/admin/notices.php` and `gutenberg/admin/updates.php`
- Updated `functions.php` and `inc/admin/admin-menu/customizer.php` for theme functionality improvements
- Updated `inc/security/modules/Honeypot.php` to add honeypot support for spam mitigation
- Updated pattern `patterns/portfolio_wireframe.php`

Fixed
- Fixed build tooling consistency and package metadata for Gutenberg block packages
- Cleaned up generated source maps and build artifacts for updated block builds

Removed
- Removed obsolete Gutenberg content column build source maps and CSS map artifacts from the repository

Notes
- This release is primarily a tooling and Gutenberg block maintenance update, with improved build automation, editor integration, and security support.

Version 3.0.0 - 2026-06-03
-------------------------
Added
- New security framework and admin UI in `inc/security/`: WAF, rate limiting, 2FA, REST API policy, IP manager, post-breach protection, request logging, whitelist management, and admin views for login security, REST API, cron guard, and post-breach handling.
- New admin tooling and integrations: `inc/admin-chat.php`, `inc/tracking-ids.php`, setup wizard support, under construction admin page, `inc/admin/admin-menu/admin-menu.php`, and support for category SEO pages, Google Merchant, content locking, and popup engine.
- New Gutenberg loader and migration support: `gutenberg/loader.php` and `gutenberg/migrations/auto-migrate-carousel-to-slider.php`.
- New WooCommerce up-sells template support: `woocommerce/single-product/up-sells.php`.
- New theme performance support: `performance/performance.php`, `inc/cache-theme-style.php`, and centralized popup frontend integration.
- Added plugin/theme updater constants and configuration in `functions.php`.

Changed
- Removed theme overrides for WooCommerce email templates to restore native WooCommerce email handling.
- Updated WooCommerce templates, hooks, and filter integration throughout cart, checkout, account, order, product, and review templates.
- Updated filter defaults:
  - `madeit_woo_single_product_col_1_class` now includes `position-relative` by default.
  - `madeit_navbar_container_class` now includes `main-navbar` by default.
  - `madeit_upper_navbar_class` now appends computed `$background['class']` to its default class list.
  - `madeit_upper_bottom_navbar_class` now appends computed `$background['class']` to its default class list.
  - `woocommerce_single_product_image_thumbnail_html` default now passes `wc_get_gallery_image_html($attachment_id, false, $key)`.
  - `woocommerce_reset_variations_link` default now includes an accessible `aria-label="Clear options"`.
- Added or updated theme define defaults in `functions.php`, including `MADEIT_VERSION=3.0.0`, updater defaults (`MADEIT_UPDATER_TYPE`, `MADEIT_UPDATER_LEVEL`, GitHub/GitLab updater defaults, `MADEIT_PLUGIN_UPDATER`, `MADEIT_PLUGIN_UPDATER_ENDPOINT`, `MADEIT_PLUGIN_UPDATER_LEVEL`), and new feature flags such as `MADEIT_FEEDBACK`, `MADEIT_ADMIN_CHAT`, `MADEIT_TRACKING_IDS`, `MADEIT_SETUP_WIZARD`, and `MADEIT_SECURITY`.
- Bumped theme version from `2.10.0` to `3.0.0`.

Fixed
- Fixed WooCommerce hook/filter integration and improved theme compatibility with updated templates.
- Fixed security/admin initialization and new feature integration for the major release.

Notes
- Major release with new security platform, admin tooling, WooCommerce layout updates, and updated theme defaults.
